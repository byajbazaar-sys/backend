/** Meta error when a free-text message is sent outside the 24-hour customer service window. */
export const META_WHATSAPP_REENGAGEMENT_ERROR_CODE = 131047;

/** WhatsApp customer service messaging window duration. */
export const WHATSAPP_CUSTOMER_SERVICE_WINDOW_MS = 24 * 60 * 60 * 1000;
